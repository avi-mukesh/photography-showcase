const ImageHeader = ({ title }) => {
    console.log(title)
    return (
        <header className="header imageHeader">
            <h1>{title}</h1>
        </header>
    )
}

export default ImageHeader
